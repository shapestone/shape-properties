# Small properties file (~10 properties, ~200 bytes)
# Used for benchmarking common config snippets

host=localhost
port=8080
debug=true
log.level=info
db.host=localhost
db.port=5432
db.name=myapp
timeout=30
max.connections=100
enabled=true
